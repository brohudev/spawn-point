---
// No props needed for this component
---

<div id="prerequisites-modal" class="fixed inset-0 z-50 flex items-center justify-center p-4">
  <!-- Modal backdrop with blur -->
  <div class="absolute inset-0 bg-rosePine-base/80 backdrop-blur-md transition-opacity duration-500"></div>
  
  <!-- Modal content -->
  <div class="relative bg-rosePine-surface border border-rosePine-highlightMed rounded-lg shadow-lg w-full max-w-md p-6 z-10 transition-opacity duration-300">
    <h2 class="text-2xl font-bold text-rosePine-love mb-4">⚠️ Warning</h2>
    <p class="text-rosePine-text mb-4">
    For Spawnpoint to work correctly, you need to have the following installed on your system:
    </p>
    <ul class="list-disc pl-5 mb-6 text-rosePine-text">
    <li class="mb-1">Python</li>
    <li class="mb-1">pip (Python package manager)</li>
    <li class="mb-1">npm (Node.js package manager)</li>
    </ul>
    <p class="text-rosePine-muted mb-6 text-sm">
    Without these dependencies, the command will fail to execute properly.
    </p>
    <button id="close-modal" class="w-full bg-rosePine-pine hover:bg-rosePine-rose text-rosePine-text py-2 px-4 rounded-md transition-colors">
    I Understand
    </button>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const modal = document.getElementById('prerequisites-modal');
    const closeButton = document.getElementById('close-modal');
    
    // Show modal on page load
    document.body.style.overflow = 'hidden'; // Prevent scrolling when modal is open
    
    if (closeButton && modal) {
      closeButton.addEventListener('click', () => {
        modal.classList.add('opacity-0', 'pointer-events-none');
        document.body.style.overflow = ''; // Re-enable scrolling
        
        // Remove the modal from the DOM after animation
        setTimeout(() => {
          modal.style.display = 'none';
        }, 300);
      });
    }
  });
</script>
